import {Alert, Col, Row} from "react-bootstrap";

const ThanksPage = () => {
  return (
    <Row>
      <Col>
        <Alert variant={'success'}>Registration success! Thanks. Await another instructions</Alert>
      </Col>
    </Row>
  );
};

export default ThanksPage;